---

- name: Pinging hosts
  command: "curl {{ansible_ssh_host}}:22"
  delegate_to: 127.0.0.1
  ignore_errors: true
  register: ping_status
 
- debug:
    var: ping_status

- setup:
       filter: ansible_*

- include: "setup.yml"

- include: "update-code.yml"

- include: "cleanup.yml"

